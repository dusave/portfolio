---
import Base from '../layouts/Base.astro';
import MotionSite from '../components/MotionSite.jsx';
---

<Base>
  {/* client:load — the scroll engine runs from the first frame, above the fold */}
  <MotionSite client:load />
</Base>
